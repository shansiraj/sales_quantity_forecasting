def set_primary_keys(df):
    """Set the primary keys for the DataFrame."""
    print ("Setting primary keys started")

    # df.set_index(['store', 'item_dept', 'date_id'], inplace=True)

    print ("Setting primary keys ended")

    return df